#include <bits/stdc++.h>
using namespace std;

int main() {
	cin.tie(0);
	ios::sync_with_stdio(0);
	int t;
	cin >> t;
	while(t--) {
		int k, n;
		cin >> k >> n;
		string s;
		cin >> s;
		int ans = 0;
        int ll = 0;
		int rr = 0;
		for(int i = 0; i < n; i++){  
            if(s[i] == '1')
        }
	}
	return 0;
}