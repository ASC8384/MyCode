#include <bits/stdc++.h>
using namespace::std;

string s;

int main()
{
    cin.tie(0);
    ios::sync_with_stdio(0);

    int t;
    cin >> t;
    while(t--){
        int n;
        cin >> n;
        cout << n << '\n';
    }

    return 0;
}

